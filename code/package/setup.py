# -*- coding: utf8 -*-
from distutils.core import setup

# Required packages are included in this list
# required_packages = []

setup(
    name="llm_fact",
    version="0.1",
    description="Local package for the LLM fact-checking project.",
    author="Matthew DeVerna",
    packages=["llm_fact"],
    # install_requires=required_packages,
)
